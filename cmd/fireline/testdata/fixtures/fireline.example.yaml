name: baseline
version: "1"
rules:
  - id: high-severity-osv
    description: >-
      OSV-Scanner findings at HIGH severity, open more than a day
    when: source.tool == "osv-scanner" && severity_raw == "HIGH"
    tolerance: 24h
